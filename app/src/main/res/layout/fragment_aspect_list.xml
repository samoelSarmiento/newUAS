<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="70dp"
    tools:context="uas.pe.edu.pucp.newuas.fragment.AspectListFragment">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/divider"
        android:text="@string/tvSeeCrits"
        android:textColor="@color/black" />

    <ListView
        android:id="@+id/lvAspects"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@color/white"
        android:dividerHeight="@dimen/half_divider"
        android:paddingEnd="@dimen/divider"
        android:paddingStart="@dimen/divider"
        android:paddingTop="@dimen/divider">

    </ListView>

</LinearLayout>
