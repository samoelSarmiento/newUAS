<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/toast_layout_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/toast_background_info"
    android:orientation="horizontal"
    android:padding="@dimen/half_divider">

    <ImageView
        android:id="@+id/image_toast"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="@dimen/half_divider"
        android:layout_marginRight="@dimen/half_divider"
        android:layout_weight="0.1"
        android:contentDescription="@string/info"
        android:src="@drawable/infoicon" />

    <TextView
        android:id="@+id/text_toast"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="0.8"
        android:gravity="center"
        android:textColor="#ffce00"
        android:textSize="14sp"
        android:textStyle="bold" />

</LinearLayout>